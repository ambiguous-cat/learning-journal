# 第一阶段：地基搭建与JavaScript核心

## 第一部分：开发环境与版本控制 (Git)

### 1. 核心概念

- Node.js: 为 JavaScript 提供服务器端运行环境的平台，也是前端工程化的基础。我们使用 LTS (长期支持) 版本以保证稳定性。
- Git: 分布式版本控制系统，代码的“时光机”。
- 分支 (Branch): Git 的核心。它像是项目的一个独立副本或“平行时空”，允许我们在不影响主线（`main` 分支）的情况下进行新功能开发或 Bug 修复。

### 2. 常用 Git 命令

- `git clone <仓库 HTTPS 或 SSH 地址>`
  - 用途: 从远程仓库（如 GitHub）完整地复制一份代码到本地。

- `git checkout -b <新分支名>`
  - 用途: 创建一个并立即切换到该新分支。遵循“先开分支，再写代码”的原则。
  - 示例: `git checkout -b topic/js-learn`

- `git checkout <已存在的分支名>`
  - 用途: 切换到另一个已经存在的分支。
  - 示例: `git checkout main`

- `git add .`
  - 用途: 将当前目录下所有的修改和新文件，添加到 Git 的“暂存区”，为下一步的提交做准备。

- `git commit -m "类型: 描述信息"`
  - 用途: 将暂存区的所有内容生成一次“快照”，永久记录在版本历史中。`-m` 后的信息至关重要。

- `git push -u origin <分支名>`
  - 用途: 首次将一个本地的新分支推送到远程仓库 `origin`。`-u` 参数会建立本地分支与远程分支的“跟踪”关系。
  - 示例: `git push -u origin topic/js-learn`
- `git push`
  - 用途: 在建立了跟踪关系后，用于将本地的后续提交推送到对应的远程分支。

### 3. 规范

- Commit 信息规范 (Conventional Commits)
  - 格式: `类型(可选范围): 描述`
  - 常用类型:
    - `feat`: 新增功能
    - `fix`: 修复 Bug
    - `docs`: 修改文档
    - `style`: 修改代码格式（不影响功能）
    - `refactor`: 代码重构
    - `test`: 增加或修改测试
  - 示例: `feat: practice string, array, and object methods`

---

## 第二部分：JavaScript

### 1. 变量声明

- `var` (不推荐使用)
  - 作用域: 函数作用域。在 `if`/`for` 块中声明的变量会“泄漏”到外部。
  - 特性: 存在变量提升（在声明前访问值为 `undefined`）。

- `let`
  - 作用域: 块级作用域 (`{}`)。变量被限制在代码块内部，更安全。
  - 特性: 不存在变量提升，在声明前访问会报错（暂时性死区）。值可以被重新赋值。

- `const`
  - 作用域: 块级作用域。
  - 特性: 声明一个常量，其值（内存地址）不可被重新赋值。
  - 关键点: 当 `const` 定义的是对象或数组时，其内部的元素或属性是可以修改的。

### 2. 核心数据类型方法

#### String (字符串)

- `trim()`
  - 用途: 移除字符串两端的空白字符。
  - 用法: `const trimmed = "  hello  ".trim(); // "hello"`

- `toUpperCase()` / `toLowerCase()`
  - 用途: 将整个字符串转换为大写或小写。
  - 用法: `const lower = "HELLO".toLowerCase(); // "hello"`

- `slice(startIndex, endIndex)`
  - 用途: 截取字符串，返回一个新字符串。不包含 `endIndex`。
  - 用法: `const sub = "JavaScript".slice(0, 4); // "Java"`

- `replace(searchValue, newValue)`
  - 用途: 查找并替换第一个匹配的子字符串。
  - 用法: `const replaced = "hello world".replace("world", "js"); // "hello js"`

- `split(separator)`
  - 用途: 将字符串按指定分隔符拆分为一个数组。
  - 用法: `const arr = "a,b,c".split(','); // ["a", "b", "c"]`

- `lastIndexOf(searchValue)`
  - 用途: 返回指定值在字符串中最后一次出现的索引。
  - 用法: `const index = "/images/avatar.png".lastIndexOf('/'); // 7`

#### Array (数组)

- 定义数组 - 'let arr = new array(5)' - 'let arr = [];'

  > 标记为 🔴 的方法会直接修改原数组。
  > 标记为 🟢 的方法不会修改原数组，而是返回一个新数组或新值。

- 🔴 `push(item)`: 在数组末尾添加一个元素，返回新长度。
- 🔴 `pop()`: 移除数组末尾的元素，返回被移除的元素。
- 🔴 `unshift(item)`: 在数组开头添加一个元素，返回新长度。
- 🔴 `shift()`: 移除数组开头的元素，返回被移除的元素。

- 🔴 `splice(start, deleteCount, ...items)`
  - 用途: 强大的数组修改工具，可以同时实现删除、替换和添加。
  - 关键点: 返回值是被删除的元素组成的数组，而不是修改后的数组。
  - 用法: `const letters = ['a', 'b', 'c']; const deleted = letters.splice(1, 1, 'X'); // letters 变为 ['a', 'X', 'c'], deleted 为 ['b']`

- 🟢 `slice(start, end)`
  - 用途: 截取数组的一部分，返回一个新数组。
  - 用法: `const nums = [1,2,3,4]; const newNums = nums.slice(1, 3); // newNums 为 [2,3], nums 不变`

- 🟢 `indexOf(item)`
  - 用途: 查找元素在数组中首次出现的索引，如果不存在则返回 `-1`。
  - 用法: `const index = ['a', 'b'].indexOf('b'); // 1`

#### Object (对象)

- 属性访问
  - 点表示法: `user.name`，用于静态、合法的属性名。
  - 方括号表示法: `user['name']`，用于动态的键名（变量）或不合法的属性名（如 `user['job title']`）。

- 🟢 `Object.keys(obj)`
  - 用途: 返回一个由对象所有键 (key) 组成的数组。
  - 用法: `const keys = Object.keys({a: 1, b: 2}); // ["a", "b"]`

#### 数组的声明式函数方法

> 核心：这些方法都不改变原数组，而是返回一个新数组或新值。

- `map(callback)`
  - **用途**：将数组中的每个元素进行“一对一”的转换，返回一个长度相同的**新数组**。
  - **回调函数** `callback(item, index, array)`：需要返回一个新的元素值。
  - **示例**：
    ```javascript
    const numbers = [1, 2, 3];
    const doubled = numbers.map((num) => num * 2);
    // doubled is [2, 4, 6]
    ```

- `filter(callback)`
  - **用途**：筛选出数组中所有满足条件的元素，返回一个**新数组**，长度可能变短。
  - **回调函数** `callback(item, index, array)`：需要返回 `true`（保留元素）或 `false`（丢弃元素）。
  - **示例**：
    ```javascript
    const scores = [50, 80, 95];
    const passing = scores.filter((score) => score >= 60);
    // passing is [80, 95]
    ```

- `reduce(callback, initialValue)`
  - **用途**：将数组中的所有元素“聚合”或“累积”成一个**单一值**。
  - **回调函数** `callback(accumulator, currentItem, currentIndex, array)`：需要返回下一次累加的值。
    - `accumulator`：累加器，是上一次回调返回的值。
    - `currentItem`：当前正在处理的元素。
  - `initialValue`：累加器的初始值。
  - **示例**：
    ```javascript
    const prices = [10, 20, 30];
    const total = prices.reduce((sum, price) => sum + price, 0);
    // total is 60. 这里的 0 是 sum 的初始值。
    ```

- **链式调用**
  - **用途**：将多个方法连接起来，形成一个清晰的数据处理流。
  - **示例**：计算有货产品的总价。
    ```javascript
    products
      .filter((p) => p.inStock) // 先筛选
      .reduce((sum, p) => sum + p.price, 0); // 再聚合
    ```
